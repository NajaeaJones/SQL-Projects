-- Write a SQL query that orders the items by price. --

SELECT price
	FROM superstore
    ORDER BY price asc;
    
-- Write your own SQL query that will show a statistic about the item prices, like a sum, average, minimum, maximum, or count.--

SELECT MAX(price)
	FROM superstore;
SELECT MIN(price)
	FROM superstore;

-- Write your own SQL query that will show a statistic about the price for items in the category of "Kitchen Supplies" the most expensive item in kitchen supplies--

SELECT MAX(price)
	FROM superstore
    WHERE category='Kitchen Supplies';
